# services/wazuh/config/opensearch_dashboards.yml
# Configuración del Wazuh Dashboard (OpenSearch Dashboards)
server.host: 0.0.0.0
server.port: 5601
opensearch.hosts: https://opsn-wazuh-indexer:9200
opensearch.ssl.verificationMode: certificate
opensearch.username: kibanaserver
opensearch.password: kibanaserver
opensearch.requestHeadersAllowlist: ["securitytenant", "Authorization"]
opensearch_security.multitenancy.enabled: false
opensearch_security.readonly_mode.roles: ["kibana_read_only"]

# TLS del servidor dashboard
server.ssl.enabled: true
server.ssl.key: /usr/share/wazuh-dashboard/certs/wazuh-dashboard-key.pem
server.ssl.certificate: /usr/share/wazuh-dashboard/certs/wazuh-dashboard.pem
opensearch.ssl.certificateAuthorities:
  ["/usr/share/wazuh-dashboard/certs/root-ca.pem"]

# Ruta por defecto al abrir el dashboard
uiSettings.overrides.defaultRoute: /app/wz-home
